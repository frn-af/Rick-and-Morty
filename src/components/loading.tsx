const Loading = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <h4 className="text-xl capitalize font-semibold">loading...</h4>
    </div>
  );
};

export default Loading;
